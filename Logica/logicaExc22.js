// Exc 22

document.write("Exercicio 22" + "<br>");

var par = 0;

while (par < 50) {
    if (par % 2 == 0) {
        
    } else if (par % 2 == 1) {
        document.write("Numero impar: " + par + " | ");
    }

    par++;
}